# Added by pipeline-template's scaffold.sh — per-machine, per-person state
# that should never be committed or shared between engineers.

# Opaque references to *your* local cmux socket (workspace:15, etc.) —
# meaningless, and actively wrong, on anyone else's machine. Regenerate with
# docs/setup-cmux-workspaces.sh after cloning.
.specify/cmux-workspaces.json
.specify/cmux-workspaces.*.json

# Generated by scaffold.sh/write_dashboard_config() — mostly deterministic
# relative paths, but still local/generated, not something to commit.
# Regenerate with `scaffold/scaffold.sh --sync` if missing after a clone.
dashboard/config.json

# Python cache
__pycache__/
*.pyc
.pytest_cache/
